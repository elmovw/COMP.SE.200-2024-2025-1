import assert from 'node:assert'
import map from '../src/map.js'

describe('map()', function () {
    it('todo')

    // it('1 + 1 = 2', function () {
    //     assert.equal(add(1, 1), 2);
    // });
});
